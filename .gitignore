# Chuks Mobile SDK — framework (core/) + native engines (ios/, android/) + docs/
# + the starter template (template/). Root-level demo/dev dirs are excluded (anchored
# with a leading /) so they don't ship, WITHOUT swallowing template/app, template/assets.

# Build outputs (regenerated by ios/build.sh, android/build.sh, dev.sh)
.out/
.out-device/
.chuks/ios-out/
.chuks/android-out/
*_exe

# Root-level demo app + local dev dirs (NOT part of the package; the shippable
# starter lives under template/).
/app/
/assets/
# Benchmarks: the built harnesses and their outputs are local, the sources are not.
# The engine comparison against Hermes is the answer to "are we faster than React
# Native", so it belongs in the repo rather than on one machine.
/benchmarks/*
!/benchmarks/boundary.chuks
!/benchmarks/bench
/benchmarks/bench/*
!/benchmarks/bench/recon
/benchmarks/bench/recon/*
!/benchmarks/bench/recon/*.chuks
!/benchmarks/bench/recon/*.js
!/benchmarks/bench/recon/run.sh
/utils/
/build/
/chuks_packages/

# macOS
.DS_Store

# NOTE: ios/yoga/*.a, ios/yoga-device/*.a, android/yoga/*.a are prebuilt Yoga
# static libs — build INPUTS, kept on purpose so the repo builds out of the box.

# Internal design docs / roadmaps — kept local, out of the repo.
docs/
.chuks/typecheck/
# Local host bridge for THIS repo's demo app; it imports app/, which is ignored too.
# Consumers get the real one from template/.chuks/entry.chuks.
.chuks/entry.chuks

# Example / demo entries — local scratch for docshots + manual testing, not part
# of the shippable package (the starter template lives under template/).
examples/

# Staging for packages that will graduate to their own repos (e.g. @chuks/filters).
# Kept local until ported out; not shipped as part of @chuks/mobile.
packages/
chuks.lock
